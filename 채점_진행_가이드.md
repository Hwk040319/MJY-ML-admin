# 팀별 Macro F1 채점 가이드 (운영진용)

이 문서만 보고 처음부터 끝까지 따라오시면 팀별 Macro F1을 확인하실 수 있습니다.
막히는 부분 있으면 언제든 연락 주세요.

---

## 0. 시작 전에 — 권한 확인 + 드라이브에 데이터 준비

아래가 미리 준비되어 있어야 합니다.

1. **GitHub 저장소 접근**: `Hwk040319/MJY-ML-admin` (비공개 저장소) — 이미 협업자로 등록되어 있으면 그대로 열립니다.
2. **구글 드라이브 데이터**: 아래 링크를 받으셨을 겁니다.
   ```
   https://drive.google.com/drive/folders/1VY6cT-2jCctboz8VOP_ZAEBpGpMFDVwN
   ```
   이 링크를 연 다음, **본인 저장공간 여유가 넉넉하면(수십 GB 이상) 아래 방법 A**를 추천합니다 — 더 확실하게 작동합니다. 저장공간이 빠듯하면(무료 15GB 계정 등) **방법 B**를 쓰세요.

   **방법 A — 사본 만들기 (저장공간 넉넉할 때 추천, 14GB 소모)**
   1. 폴더 안의 `battery-colab-grouped.tar` 파일을 우클릭 → **"사본 만들기"**
   2. 생긴 파일 이름이 `battery-colab-grouped.tar의 사본` 같은 식으로 되어있을 텐데, **"의 사본" 부분을 지워서 원래 이름(`battery-colab-grouped.tar`)과 똑같이 맞춰주세요.**
   3. 이 사본은 보통 "내 드라이브" 맨 위(루트)에 생깁니다. 아래 3-2번 코드에서 경로를 다음과 같이 씁니다:
      ```python
      !tar -xf "/content/drive/MyDrive/battery-colab-grouped.tar" -C /content/battery-project-grouped
      ```

   **방법 B — 바로가기 추가 (저장공간 빠듯할 때, 용량 0 소모)**
   1. 폴더 이름(`MJY_ML_운영진전용`) 위에서 우클릭 → **"드라이브에 바로가기 추가"**
   2. 3-2번 코드는 아래처럼 폴더 경로를 그대로 씁니다:
      ```python
      !tar -xf "/content/drive/MyDrive/MJY_ML_운영진전용/battery-colab-grouped.tar" -C /content/battery-project-grouped
      ```

   둘 중 뭘 했든, 이 단계를 건너뛰면 3-2번에서 파일을 못 찾는 오류가 납니다 — 링크만 열어본 상태로는 "내 드라이브" 안에 안 잡히기 때문입니다.

   **참고**: GitHub 로그인 계정과 여기서 쓰는 구글 계정은 서로 달라도 됩니다(완전히 별개 시스템). 학교 이메일(구글 워크스페이스) 계정에서 사본 만들기·바로가기·Colab GPU 등이 막혀 있으면, 그냥 개인 지메일 계정으로 이 단계를 진행하면 됩니다.

두 가지 다 확인됐으면 시작하세요. (아래 3-2번 코드는 방법 B 기준으로 적어뒀습니다 — 방법 A를 쓰셨으면 위 경로로 바꿔서 실행하세요.)

---

## 1. 준비물 체크리스트

- [ ] 위 두 권한 확인 완료
- [ ] Google Colab 접속 가능 (colab.research.google.com, 위 권한 받은 **같은 구글 계정**으로 로그인)
- [ ] 채점할 팀의 `best_model.pt` 파일 (이메일로 받은 것)

---

## 2. 팀 제출 파일을 드라이브에 정리하기

이메일로 받은 `best_model.pt`를 로컬로 안 내려받고 **바로 드라이브에 저장**하는 게 제일 편합니다.

1. 구글 드라이브의 `MJY_ML_운영진전용` 폴더 안에 `submissions` 폴더가 있는지 확인 (없으면 새로 만들기)
2. 그 안에 팀별로 폴더를 만듭니다: `submissions/1조`, `submissions/2조`, ...
3. Gmail에서 첨부파일(`best_model.pt`) 위에 마우스를 올리면 나오는 **"드라이브에 저장"** 아이콘을 눌러서, 해당 팀 폴더 안에 저장합니다.
4. 파일 이름은 정확히 `best_model.pt`여야 합니다 (팀 이름이 붙은 파일명이면 그 폴더 안에서 `best_model.pt`로 이름을 바꿔주세요).

결과적으로 이런 모양이 되어야 합니다:
```
MJY_ML_운영진전용/
  submissions/
    1조/best_model.pt
    2조/best_model.pt
    ...
```

---

## 3. Colab에서 채점 돌리기

colab.research.google.com 에서 새 노트북을 열고, 아래 코드를 순서대로 한 셀씩 붙여넣어 실행하세요 (각 셀 실행 후 완료될 때까지 기다렸다가 다음 셀로 넘어가세요).

### 3-1. 내 드라이브 연결
```python
from google.colab import drive
drive.mount('/content/drive')
```
화면에 나오는 안내대로 본인 구글 계정으로 로그인/허용하시면 됩니다.

### 3-2. 원본 데이터 압축 풀기 (몇 분 걸립니다)
```python
!mkdir -p /content/battery-project-grouped
!tar -xf "/content/drive/MyDrive/MJY_ML_운영진전용/battery-colab-grouped.tar" -C /content/battery-project-grouped
!ls /content/battery-project-grouped/participant/data_grouped
```
`private_test`, `public_val`, `train` 세 개 폴더가 보이면 정상입니다.

### 3-3. 채점 코드 받기
```python
!git clone https://github.com/Hwk040319/MJY-ML-admin.git
%cd MJY-ML-admin
!pip install -q -r requirements.txt
```

### 3-4. 채점 실행
```python
!python batch_score.py \
    --submissions-dir "/content/drive/MyDrive/MJY_ML_운영진전용/submissions" \
    --data-root /content/battery-project-grouped/participant/data_grouped
```

이 셀이 끝나면 화면에 이런 식으로 팀별 결과가 쭉 뜹니다:
```
[1조] macro_f1=0.XXXX  accuracy=0.XXXX  (N장 채점, 누락 M장)
[2조] macro_f1=0.XXXX  accuracy=0.XXXX  (N장 채점, 누락 M장)
...
```

**이 `macro_f1` 값이 원하시는 팀별 점수입니다.**

---

## 4. 주의사항

- **⚠️ 지금은 `macro_f1` 값만 참고하세요.** 같이 출력되는 `performance_score`(성능점수)·`total_score`(총점)·`leaderboard.csv`의 순위는 채점 공식이 아직 최종 확정 전이라 그대로 믿으면 안 됩니다. 확정되면 다시 안내드리겠습니다.
- 특정 팀 제출이 없거나 파일이 깨져 있으면 `macro_f1=0.0000`으로 나오고 "체크포인트 없음" 또는 "채점 실패"라고 표시됩니다 — 오류가 아니라 정상 동작입니다.
- 채점에 쓰는 데이터(특히 `private_test`)와 이 저장소는 **참가자에게 절대 공유하거나 외부에 유출하면 안 됩니다.**
- Colab 세션이 도중에 끊기면(특히 압축 해제 오래 걸릴 때) 3-1번부터 다시 실행하시면 됩니다 — 다시 해도 몇 분이면 끝납니다.
- 한 팀만 다시 채점하고 싶으면, `submissions` 폴더 안에 그 팀 폴더 하나만 남기거나, `--submissions-dir`을 그 팀 폴더의 상위 경로로 지정하면 됩니다.

---

궁금한 점 있으면 언제든 연락 주세요.
