# 팀별 Macro F1 채점 가이드 (운영진용)

이 문서만 보고 처음부터 끝까지 따라오시면 팀별 Macro F1을 확인하실 수 있습니다.
막히는 부분 있으면 언제든 연락 주세요.

---

## 0. 시작 전에 — 권한 확인 + 데이터 준비

아래가 미리 준비되어 있어야 합니다.

1. **GitHub 저장소 접근**: `Hwk040319/MJY-ML-admin` — public 저장소라 별도 로그인/권한 없이 그대로 열립니다.
2. **구글 드라이브 데이터(이미지)**: 아래 링크를 받으셨을 겁니다.
   ```
   https://drive.google.com/drive/folders/1VY6cT-2jCctboz8VOP_ZAEBpGpMFDVwN
   ```
   이 링크를 연 다음:
   1. 폴더 안의 `battery-colab-grouped.tar` 파일을 우클릭 → **"사본 만들기"**
   2. 생긴 파일 이름이 `battery-colab-grouped.tar의 사본` 같은 식으로 되어있을 텐데, **"의 사본" 부분을 지워서 원래 이름(`battery-colab-grouped.tar`)과 똑같이 맞춰주세요.**
   3. 이 사본은 "내 드라이브" 맨 위(루트)에 생깁니다.

   이 단계를 건너뛰면 2-2번에서 파일을 못 찾는 오류가 납니다.

   **참고**: GitHub 로그인 계정과 여기서 쓰는 구글 계정은 서로 달라도 됩니다(완전히 별개 시스템). 학교 이메일(구글 워크스페이스) 계정에서 사본 만들기·Colab GPU 등이 막혀 있으면, 그냥 개인 지메일 계정으로 이 단계를 진행하면 됩니다.
3. **비공개 정답 파일**: `private_labels.csv` — 위 드라이브 tar 안에는 들어있지 않습니다. 정답 유출 방지를 위해 운영진(hwk040319)이 카톡/이메일 등 **별도 경로로 직접** 전달하는 작은 CSV 파일입니다. 아직 못 받으셨으면 먼저 요청해서 받아두세요.
4. **팀별 체크포인트 모으기**: 이메일 등으로 받은 팀별 `best_model.pt`를 **내 드라이브의 `MJY` 폴더**(`내 드라이브` 맨 위에 `MJY`라는 새 폴더 생성) 안에 전부 넣어두세요. 파일 이름은 팀을 구분할 수만 있으면 자유롭게 지으면 됩니다 (예: `1조.pt`, `2조_best_model.pt`). 팀별 하위 폴더(`MJY/1조/best_model.pt`)로 넣어도 됩니다 — 하위 폴더까지 전부 찾아서 채점합니다.

네 가지 다 확인됐으면 시작하세요.

---

## 1. 준비물 체크리스트

- [ ] 위 네 가지 준비 완료 (GitHub 접근 / 드라이브 tar 사본 / private_labels.csv / MJY 폴더에 체크포인트 모으기)
- [ ] Google Colab 접속 가능 (colab.research.google.com, 위 권한 받은 **같은 구글 계정**으로 로그인)

---

## 2. Colab에서 채점 돌리기 — 런타임 → 모두 실행 한 번이면 끝

[test_private.ipynb](test_private.ipynb)를 Colab에서 열고(위 GitHub의 "Open in Colab" 배지를 누르면 됩니다),
**런타임 → 모두 실행**을 누르세요. 중간에 딱 두 번만 화면을 봐주면 됩니다:

1. 드라이브 접근 허용 창 (아래 2-2번 셀) — 로그인/허용
2. `private_labels.csv` 업로드 창 (아래 2-4번 셀) — 운영진에게 받은 파일 선택

이 두 번만 처리해주면 나머지는 자동으로 끝까지 실행되면서, `MJY` 폴더 안에 넣어둔 모든 팀의
Macro F1이 순서대로 출력됩니다. 아래는 셀별 설명입니다 (셀을 하나씩 직접 붙여넣어 실행해도 결과는 같습니다).

### 2-1. 채점 코드 받기
```python
!git clone https://github.com/Hwk040319/MJY-ML-admin.git
%cd MJY-ML-admin
!pip install -q -r requirements.txt
```

### 2-2. 내 드라이브 연결 + 원본 데이터 압축 풀기 (몇 분 걸립니다)
```python
from google.colab import drive
drive.mount('/content/drive')

!mkdir -p /content/battery-project-grouped
!tar -xf "/content/drive/MyDrive/battery-colab-grouped.tar" -C /content/battery-project-grouped
!ls /content/battery-project-grouped/participant/data_grouped
```
`private_test`, `public_val`, `train` 세 개 폴더가 보이면 정상입니다. (이 시점의 `private_test`에는 이미지만 있고 정답은 아직 없는 게 정상입니다 — 다음 단계에서 넣습니다.)

### 2-3. (2-1을 셀 하나씩 실행 중이라면 생략) — 위에서 이미 클론 완료

### 2-4. 비공개 정답 파일 업로드 (★ 세션당 한 번만)
```python
from google.colab import files
import shutil

uploaded = files.upload()   # 운영진에게 받은 private_labels.csv 선택
shutil.move(list(uploaded.keys())[0],
            "/content/battery-project-grouped/participant/data_grouped/private_test/private_labels.csv")
print('정답 파일 배치 완료')
```
셀을 실행하면 파일 선택 창이 뜹니다 — 0번에서 받아둔 `private_labels.csv`를 선택하세요.

### 2-5. 팀별 Macro F1 한 번에 채점 (★ 새 팀 추가 시 이 셀만 다시 실행)
```python
from pathlib import Path

ckpt_dir = Path("/content/drive/MyDrive/MJY")
ckpt_files = sorted(p for p in ckpt_dir.rglob("*.pt") if p.is_file())

if not ckpt_files:
    print(f"체크포인트(.pt)를 찾을 수 없습니다: {ckpt_dir}")
    print("이 폴더 안에 팀별 best_model.pt 파일을 넣은 뒤 이 셀을 다시 실행하세요.")
else:
    print(f"{len(ckpt_files)}개 체크포인트 발견, 순서대로 채점합니다.\n")
    for ckpt in ckpt_files:
        print(f"\n{'='*20} {ckpt.relative_to(ckpt_dir)} {'='*20}")
        !python score_checkpoint.py \
            --checkpoint "{ckpt}" \
            --data-root /content/battery-project-grouped/participant/data_grouped
```
이 셀 하나가 `MJY` 폴더 안 모든 `.pt` 파일을 찾아 순서대로 채점합니다. 각 팀마다 이렇게 결과가 뜹니다:
```
==================== 1조.pt ====================
==================================================
체크포인트: 1조.pt
학습 epoch 4, 학습 당시 Public Val Macro F1 0.8850
채점 이미지 수: 33482장 (누락 0장)
Macro F1 : 0.XXXX
Accuracy : 0.XXXX
Class F1 : 초기 0.XXXX  중기 0.XXXX  후기 0.XXXX
==================================================

==================== 2조.pt ====================
...
```

**팀별 `Macro F1` 값이 그 팀의 점수입니다.** 나중에 팀을 더 추가하려면 드라이브 `MJY` 폴더에 파일만 넣고 **이 2-5번 셀만 다시 실행**하면 됩니다 (앞 단계는 다시 안 해도 됨, 같은 세션이 살아있는 동안은).

---

## 3. 주의사항

- 최종 순위·등수 계산 방식은 아직 확정 전입니다. 지금은 **팀별 `Macro F1` 값만** 기록해두시면 되고, 나중에 공식이 확정되면 그 값들로 한 번에 순위를 매길 예정입니다.
- 체크포인트 파일이 깨져 있거나 형식이 안 맞으면 그 팀 부분에서 오류 메시지가 뜹니다 (다른 팀 채점은 계속 진행됩니다) — 그 팀에게 파일을 다시 받아야 합니다.
- 채점에 쓰는 데이터(특히 `private_test`, `private_labels.csv`)와 이 저장소는 **참가자에게 절대 공유하거나 외부에 유출하면 안 됩니다.** `private_labels.csv`는 본인 컴퓨터에도 계속 두지 말고, 다 쓰면 지워주세요.
- Colab 세션이 도중에 끊기면(특히 2-2번 압축 해제 중) 2-1번부터 다시 실행하시면 됩니다 — 드라이브 `MJY` 폴더 내용은 그대로 남아있으니 2-5번은 바로 다시 끝납니다.

---

궁금한 점 있으면 언제든 연락 주세요.
