# 팀별 Macro F1 채점 가이드 (운영진용)

이 문서만 보고 처음부터 끝까지 따라오시면 팀별 Macro F1을 확인하실 수 있습니다.
막히는 부분 있으면 언제든 연락 주세요.

---

## 0. 시작 전에 — 권한 확인 + 드라이브에 데이터 준비

아래가 미리 준비되어 있어야 합니다.

1. **GitHub 저장소 접근**: `Hwk040319/MJY-ML-admin` (비공개 저장소) — 이미 협업자로 등록되어 있으면 그대로 열립니다.
2. **구글 드라이브 데이터**: 아래 링크를 받으셨을 겁니다.
   ```
   https://drive.google.com/drive/folders/1VY6cT-2jCctboz8VOP_ZAEBpGpMFDVwN
   ```
   이 링크를 연 다음:
   1. 폴더 안의 `battery-colab-grouped.tar` 파일을 우클릭 → **"사본 만들기"**
   2. 생긴 파일 이름이 `battery-colab-grouped.tar의 사본` 같은 식으로 되어있을 텐데, **"의 사본" 부분을 지워서 원래 이름(`battery-colab-grouped.tar`)과 똑같이 맞춰주세요.**
   3. 이 사본은 "내 드라이브" 맨 위(루트)에 생깁니다.

   이 단계를 건너뛰면 2-2번에서 파일을 못 찾는 오류가 납니다.

   **참고**: GitHub 로그인 계정과 여기서 쓰는 구글 계정은 서로 달라도 됩니다(완전히 별개 시스템). 학교 이메일(구글 워크스페이스) 계정에서 사본 만들기·Colab GPU 등이 막혀 있으면, 그냥 개인 지메일 계정으로 이 단계를 진행하면 됩니다.

두 가지 다 확인됐으면 시작하세요.

---

## 1. 준비물 체크리스트

- [ ] 위 두 권한 확인 완료
- [ ] Google Colab 접속 가능 (colab.research.google.com, 위 권한 받은 **같은 구글 계정**으로 로그인)
- [ ] 채점할 팀의 `best_model.pt` 파일이 본인 컴퓨터에 저장되어 있을 것 (이메일 첨부파일을 다운로드해두면 됩니다)

---

## 2. Colab에서 채점 돌리기 — 파일 업로드하면 바로 점수 나옵니다

colab.research.google.com 에서 새 노트북을 열고, 아래 코드를 순서대로 한 셀씩 붙여넣어 실행하세요 (각 셀 실행 후 완료될 때까지 기다렸다가 다음 셀로 넘어가세요). **2-1~2-3번은 세션당 한 번만** 하면 되고, **팀마다 반복하는 건 2-4번뿐**입니다.

### 2-1. 내 드라이브 연결
```python
from google.colab import drive
drive.mount('/content/drive')
```
화면에 나오는 안내대로 본인 구글 계정으로 로그인/허용하시면 됩니다.

### 2-2. 원본 데이터 압축 풀기 (몇 분 걸립니다)
```python
!mkdir -p /content/battery-project-grouped
!tar -xf "/content/drive/MyDrive/battery-colab-grouped.tar" -C /content/battery-project-grouped
!ls /content/battery-project-grouped/participant/data_grouped
```
`private_test`, `public_val`, `train` 세 개 폴더가 보이면 정상입니다.

### 2-3. 채점 코드 받기
```python
!git clone https://github.com/Hwk040319/MJY-ML-admin.git
%cd MJY-ML-admin
!pip install -q -r requirements.txt
```

### 2-4. 체크포인트 업로드 → 바로 채점 (★ 팀마다 이 셀만 반복 실행)
```python
from google.colab import files
uploaded = files.upload()   # 여기서 팀의 best_model.pt 파일 선택
ckpt_name = list(uploaded.keys())[0]

!python score_checkpoint.py --checkpoint "{ckpt_name}" --data-root /content/battery-project-grouped/participant/data_grouped
```
셀을 실행하면 파일 선택 창이 뜹니다 — 채점할 팀의 `best_model.pt`를 골라 업로드하세요. 잠시 후 이렇게 결과가 뜹니다:
```
==================================================
체크포인트: best_model.pt
학습 epoch 4, 학습 당시 Public Val Macro F1 0.8850
채점 이미지 수: 33482장 (누락 0장)
Macro F1 : 0.XXXX
Accuracy : 0.XXXX
Class F1 : 초기 0.XXXX  중기 0.XXXX  후기 0.XXXX
==================================================
```

**`Macro F1` 값이 그 팀의 점수입니다.** 다음 팀을 채점하려면 **2-4번 셀을 다시 실행**해서 그 팀 파일을 업로드하면 됩니다 (2-1~2-3번은 다시 안 해도 됨, 같은 세션이 살아있는 동안은).

---

## 3. 주의사항

- 최종 순위·등수 계산 방식은 아직 확정 전입니다. 지금은 **팀별 `Macro F1` 값만** 기록해두시면 되고, 나중에 공식이 확정되면 그 값들로 한 번에 순위를 매길 예정입니다.
- 체크포인트 파일이 깨져 있거나 형식이 안 맞으면 오류 메시지가 뜹니다 — 그 팀에게 파일을 다시 받아야 합니다.
- 채점에 쓰는 데이터(특히 `private_test`)와 이 저장소는 **참가자에게 절대 공유하거나 외부에 유출하면 안 됩니다.**
- Colab 세션이 도중에 끊기면(특히 2-2번 압축 해제 중) 2-1번부터 다시 실행하시면 됩니다 — 다시 해도 몇 분이면 끝납니다.
- 여러 팀을 한 번에 몰아서 채점하고 싶다면 말씀해주세요 — 필요하면 일괄 처리용 버전을 별도로 만들어드릴 수 있습니다.

---

궁금한 점 있으면 언제든 연락 주세요.
